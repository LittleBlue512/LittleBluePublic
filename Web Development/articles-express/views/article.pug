extends layout

block content
    h2= article.title
    h5= "By " + article.author
    p(style="font-size: 24px")= article.content
    a.btn.btn-light(href="/articles/edit/" + article._id) Edit
    |
    |
    a.btn.btn-danger.delete-article(href="#" data-id=article._id) Delete
